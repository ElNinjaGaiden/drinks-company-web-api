import { AwilixContainer } from 'awilix';

// eslint-disable-next-line no-unused-vars
const registerCustomDependencies = (container: AwilixContainer) => {
	console.log('No custom dependencies to register, site running as core version');
};

export default registerCustomDependencies;
